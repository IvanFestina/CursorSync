# Cursor Settings Sync

Проект для синхронизации настроек Cursor между устройствами macOS.

## 📋 Что синхронизируется

- ✅ Настройки (`settings.json`)
- ✅ Горячие клавиши (`keybindings.json`)
- ✅ Snippets (пользовательские сниппеты)
- ✅ Список установленных расширений
- ✅ Настройки UI и темы (через `globalStorage`)

## 🚀 Быстрый старт

### Первое устройство (экспорт настроек)

```bash
chmod +x export.sh
./export.sh
```

После выполнения скрипта:
1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Экспорт настроек Cursor"
   git push
   ```

### Второе устройство (импорт настроек)

1. Клонируйте репозиторий или выполните `git pull`
2. Запустите скрипт импорта:
   ```bash
   chmod +x import.sh
   ./import.sh
   ```
3. Перезапустите Cursor

## 📁 Структура проекта

```
CursorSync/
├── settings/              # Экспортированные настройки
│   ├── settings.json      # Основные настройки
│   ├── keybindings.json   # Горячие клавиши
│   ├── snippets/          # Пользовательские сниппеты
│   └── globalStorage/     # Настройки UI и тем
├── extensions/
│   └── extensions.txt     # Список установленных расширений
├── export.sh              # Скрипт экспорта
├── import.sh              # Скрипт импорта
└── README.md              # Этот файл
```

## 🔄 Обновление настроек

Когда вы изменили настройки на одном устройстве:

1. Выполните `./export.sh`
2. Закоммитьте и запушьте изменения
3. На других устройствах выполните `git pull` и `./import.sh`

## ⚠️ Важные замечания

- Скрипт импорта автоматически создает резервную копию ваших текущих настроек
- Если команда `cursor` не найдена в PATH, скрипт попытается использовать полный путь к приложению
- Проект настроен только для macOS

## 🛠️ Устранение неполадок

### Команда `cursor` не найдена

Убедитесь, что Cursor установлен и добавлен в PATH. Альтернативно используйте полный путь к исполняемому файлу:
```bash
/Applications/Cursor.app/Contents/Resources/app/bin/cursor --list-extensions
```

### Расширения не устанавливаются автоматически

Установите их вручную, используя список из `extensions/extensions.txt`:
```bash
cursor --install-extension <extension-id>
```

### Настройки не применяются

1. Убедитесь, что вы перезапустили Cursor
2. Проверьте, что файлы были скопированы в правильную директорию:
   ```bash
   ~/Library/Application Support/Cursor/User/
   ```

## 📝 Лицензия

Этот проект создан для личного использования. Используйте на свой страх и риск.

